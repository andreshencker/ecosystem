# jtrade frontend — production build defaults.
# The authoritative values are passed as build ARGs from docker-compose.prod.yml
# (VITE_API_BASE, VITE_GRAPIFLY_ID_URL, VITE_GRAPIFLY_WEB_URL) and override
# anything set here. Only add a key here if it must ship in every prod build
# regardless of compose.
