// src/payments/providers/stripe/stripe.constants.ts

export const STRIPE_PROVIDER_KEY = 'stripe' as const;
export const STRIPE_DISPLAY_NAME = 'Stripe';
export const STRIPE_DESCRIPTION =
  'Stripe — online payment processing via API keys';
export const STRIPE_CONNECTION_TYPE = 'api_key' as const;
